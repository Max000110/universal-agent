---
name: session-validate
description: Validates stored session cookie structure and token expiry
command: session-validate
version: 1.0.0
---
# Session Validate Skill

Checks stored cookie payloads in the encrypted vault to verify format validity (Header string vs JSON), required token parameters, and provider compatibility.
Trigger via `/session validate`.
